# Legacy Agents (deprecated)

These modules (`scribe/`, `proto/`, `trace/`) are the original single-agent implementations.
They are **superseded** by `../pipeline/agents/` which the orchestrator uses.

Do NOT import from this directory for new features. These files exist only for
reference and will be removed in a future cleanup PR.
